#include <stdio.h>

int main() {
    int number, result;

    // Read 0 from the user
    printf("Enter 0: ");
    scanf("%d", &number);

    // Subtract 1 from the number
    result = number - 1;

    // Print the result
    printf("The result is: %d\n", result);

    return 0;
}
